import { SubtopicFilterPipe } from './subtopic-filter.pipe';

describe('SubtopicFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SubtopicFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
